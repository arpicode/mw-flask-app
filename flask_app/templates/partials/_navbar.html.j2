<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container-fluid">

        <a href="/" class="navbar-brand">Flask App</a>
        <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="navbarCollapse" class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if active_page == 'home' else '' }}" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if active_page == 'about' else '' }}" href="/about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if active_page == 'articles' else '' }}"
                        href="/articles">Articles</a>
                </li>
            </ul>

            <ul class="navbar-nav">
                {% if not session.logged_in %}
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if active_page == 'register' else '' }}"
                        href="/register">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if active_page == 'login' else '' }}" href="/login">Login</a>
                </li>          
                {% else %}
                <li class="nav-item">
                    <a class="nav-link {{ 'active' if active_page == 'dashboard' else '' }}" href="/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Logout</a>
                </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>